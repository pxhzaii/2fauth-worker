{
    "name": "2fauth-worker",
    "version": "1.0.2",
    "description": "A Secure 2FA Management Tool running on Cloudflare Workers",
    "author": "nap0o",
    "license": "AGPL-3.0",
    "private": true,
    "scripts": {
        "postinstall": "npm install --prefix backend && npm install --prefix frontend",
        "dev": "npm run dev --prefix backend & npm run dev --prefix frontend"
    }
}